export type userInfo = {
        username: string;
        fallower: string;
        fallowing: string;
        post: string;
        userimage: string;
}

export type remixta = {
    id: number
    name: string
    title: string
    body: string
    img: string
}